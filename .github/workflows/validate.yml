name: validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
      - name: Install locked dependencies and package
        run: |
          pip install -r requirements.lock
          pip install --no-build-isolation --no-deps -e .
      - name: Run tests
        run: python -m unittest discover -s tests -v
      - name: Verify publication provenance when present
        run: python tools/validate_publication.py --require-git
      - name: Validate project
        shell: pwsh
        run: ./tools/validate-project.ps1 -SkipDocker
      - name: Build Docker image
        run: docker build -t embeddings-benchmark:ci .
      - name: Run dense benchmark in Docker
        run: docker run --rm --network none embeddings-benchmark:ci
